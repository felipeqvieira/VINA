                    Extrair Membros

    A função `extrair_membros` tem como objetivo extrair um membro específico da estrutura `Archive` e salvá-lo em um arquivo separado. Vamos analisar suas características e estruturas:

    - `archive`: É um ponteiro para a estrutura `Archive` que contém os membros.
    - `nome_membro`: É o nome do membro a ser extraído.

    A função realiza as seguintes operações:

    1. Encontra o índice do membro com o nome `nome_membro` dentro da estrutura `Archive` usando a função `encontrar_nome`. Se o membro não for encontrado, exibe uma mensagem de erro usando `perror` e retorna um código indicando que o arquivo não foi encontrado.

    2. Abre o arquivo `nome_membro` em modo de escrita binária usando `fopen`. Se ocorrer algum erro na criação do arquivo, exibe uma mensagem de erro usando `perror` e retorna um código de erro.

    3. Obtém o ponteiro para o membro encontrado com base no índice obtido.

    4. Escreve os dados do membro no arquivo usando `fwrite`, especificando o tamanho de um membro (`sizeof(Membro)`) e gravando apenas um membro.

    5. Fecha o arquivo.

    Essa função permite extrair um membro específico da estrutura `Archive` e salvá-lo em um arquivo separado. Ela utiliza o índice retornado pela função `encontrar_nome` para acessar o membro correto na estrutura `Archive` e, em seguida, grava os dados desse membro em um arquivo separado usando `fwrite`. Isso possibilita a extração individual de membros do arquivo `archive.vpp`.